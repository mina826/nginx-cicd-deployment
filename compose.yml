services:
  web:
    image: ${IMAGE_REF:-nginx-cicd-demo:v0.1.0}
    container_name: nginx-cicd-demo
    restart: unless-stopped

    ports:
      - "8080:80"

    labels:
      project: nginx-cicd-demo
      managed-by: docker-compose
